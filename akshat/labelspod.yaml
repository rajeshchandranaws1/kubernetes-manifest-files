apiVersion: v1
kind: Pod  
metadata:
  name: labelspod
  labels:
    env: development
    class: pods
    company: edtik
    name: akshat
spec:
  containers:
    - name: c00
      image: ubuntu
      command: ["/bin/bash", "-c", "while true; do echo welcome to the training; sleep 10; done"]

